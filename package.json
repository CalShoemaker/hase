{
  "name": "hase-susskartoffel",
  "version": "1.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "boot": "(cd packages/nest-api && json-server --watch data/db.json) & (cd packages/react-host && npm run start)"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.0-alpha.6"
  }
}
